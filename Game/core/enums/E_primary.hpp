#ifndef ENUM_PRIMARY_HPP
#define ENUM_PRIMARY_HPP

#include <cstdint>

namespace core::enums {

enum class Primary : uint8_t {
    Constitution = 0,
    Dexterity = 1,
    Intelligence = 2,
    Strength = 3,
};
}
#endif
